# DEPRECATED: This file is deprecated. Please use `uv sync --package area-occupancy-simulator` instead.
# The simulator now uses a pyproject.toml file in the simulator/ directory.
# This file is kept for backward compatibility only and may be removed in a future version.

# Flask web framework and CORS support
Flask>=3.0.0
Flask-Cors>=4.0

# Home Assistant (provides homeassistant.const, homeassistant.util.dt, PyYAML, SQLAlchemy, etc.)
# See https://raw.githubusercontent.com/home-assistant/core/refs/heads/dev/requirements.txt
# for full list of dependencies included with Home Assistant
homeassistant>=2025.12.1

# numpy is required by custom_components.area_occupancy.db.correlation
# Even though the simulator doesn't use db modules directly, Python imports them
# transitively when loading custom_components, so numpy must be available
numpy>=2.3.2